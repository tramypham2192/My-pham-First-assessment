1) What does the `rm` command in the terminal do?

Your answer:
rm command is used to delete files.

------------------
2) What terminal command would you use to list all the files and folder in the 
   current directory?

Your answer:
ls -a or ls

------------------
3) Explain the difference between these two commands in the terminal:
   
   $ cd Users
   $ cd /Users

Your answer:
Prepending a \ or / to a path specifies it as being absolute

------------------
4) Describe what git is and what it is used for.

Your answer:
git is a version control system. People often use git when programming to save different versions of their code

------------------
5) Describe what GitHub is and what it is used for.

Your answer:
GitHub is similar to a social platform for coders, software engineers... People use GitHub to collaborate on coding projects

------------------
6) Which of these commands sets up a new git repository?
    a) git clone 
    b) git pull
    c) git init
    d) git status

Your answer:
c 
------------------
7) You want to check that you have properly added all the files you want to be
   tracked in git BEFORE you make a commit. Which command will do this?
     a) git push
     b) git commit
     c) git status
     d) git remote

Your answer:
git status 

------------------
8) What is the difference between `git add` and `git commit`?

Your answer:
git add is used to add files to staging area
git commit is used to save a version into git history, similar to "take a snapshot" of that version

------------------
9) What does the `git push` command do?

Your answer:
git push is used to send the code version that has just been committed to a GitHub repository 

------------------
10) When would you use the `git clone` command?

Your answer:
git clone is used to make a copy of a GitHub repository into my local computer

